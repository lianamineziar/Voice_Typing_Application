# Voice_Typing_Application
Voice Typing is a simple desktop application built using Python and Tkinter that allows users to type text using their voice.   It listens through the system microphone, converts speech into text using speech recognition, and automatically types the recognized text wherever the cursor is placed.
